{
  "name": "unraid-toolkit-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Monorepo for unraid-toolkit: a core SDK with thin CLI and MCP wrappers over the Unraid GraphQL API",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo",
    "build": "tsc --build",
    "build:clean": "tsc --build --clean && tsc --build",
    "typecheck": "tsc --build",
    "codegen": "graphql-codegen --config codegen.ts",
    "codegen:check": "graphql-codegen --config codegen.ts && git diff --exit-code -- packages/sdk/src/unraid/generated.ts",
    "lint": "eslint packages/*/src",
    "lint:fix": "eslint packages/*/src --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "pnpm -r run test",
    "knip": "knip",
    "syncpack:check": "syncpack lint",
    "syncpack:fix": "syncpack fix-mismatches",
    "check": "pnpm run codegen:check && pnpm run build && pnpm run lint && pnpm run format:check && pnpm run syncpack:check && pnpm run knip && pnpm run test",
    "changeset": "changeset",
    "version": "changeset version && pnpm install --lockfile-only",
    "release": "pnpm run build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@eslint/js": "^10.0.1",
    "@graphql-codegen/cli": "^5.0.7",
    "@graphql-codegen/typescript": "^4.1.6",
    "@graphql-codegen/typescript-operations": "^4.6.1",
    "@types/node": "^24.12.4",
    "eslint": "^10.4.1",
    "eslint-config-prettier": "^10.1.8",
    "graphql": "^16.14.0",
    "husky": "^9.1.7",
    "knip": "^5.66.0",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.3",
    "syncpack": "^14.0.4",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.60.0",
    "vitest": "^4.1.7"
  },
  "packageManager": "pnpm@10.32.1"
}
