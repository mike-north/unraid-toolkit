{
  "name": "unraid-cli",
  "version": "0.1.0",
  "description": "Unraid toolkit umbrella — re-exports the SDK, CLI, and MCP server, and ships the `unraid` command.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "unraid": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@unraid-cli/cli": "workspace:*",
    "@unraid-cli/mcp": "workspace:*",
    "@unraid-cli/sdk": "workspace:*"
  }
}
